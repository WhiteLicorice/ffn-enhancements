// enums/SupportedFormats.ts

/**
 * Defines the formats we support for downloading.
 */
export enum SupportedFormats {
    HTML = 'html',
    MARKDOWN = 'md',
    EPUB = 'epub',
    MOBI = 'mobi',
    PDF = 'pdf',
}