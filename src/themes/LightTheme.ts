import { Theme } from '../enums/Theme';
import { IThemeDefinition } from '../interfaces/IThemeDefinition';

export const LightTheme: IThemeDefinition = {
    name: Theme.LIGHT,
    label: 'Light',
    isDark: false,
    tokens: {},
    colorMap: {},
};
